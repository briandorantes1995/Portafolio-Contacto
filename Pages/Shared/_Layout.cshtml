@using System.Security.Claims

@{
        var authenticated = User.Identity != null && User.Identity.IsAuthenticated;
        var admin = User.IsInRole("Admin");
        var avatarUrl = User.FindFirst("AvatarUrl")?.Value;
        var userIdClaim = User.FindFirst(ClaimTypes.NameIdentifier)?.Value;
        int.TryParse(userIdClaim, out var userId);
}
<!DOCTYPE html>
<html class="h-full bg-gray-900" lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Portafolio</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
</head>
<body class="h-full">
<header>
    <nav class="navbar navbar-expand-sm navbar-toggleable-sm navbar-light bg-white border-bottom box-shadow mb-3">
        <div class="container">
            <a class="navbar-brand" asp-area="" asp-page="/Index">Portafolio</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target=".navbar-collapse" aria-controls="navbarSupportedContent"
                    aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="navbar-collapse collapse d-sm-inline-flex justify-content-between">
                <ul class="navbar-nav flex-grow-1">
                    <li class="nav-item">
                        <a class="nav-link text-dark" asp-area="" asp-page="/Index">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-dark" asp-area="" asp-page="/ContactMessages/Create">Contacto</a>
                    </li>
                    @if (authenticated)
                    {
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" role="button" data-bs-toggle="dropdown">
                                <img src="@(string.IsNullOrEmpty(avatarUrl) ? "./assets/no-image.png" : avatarUrl)" width="32" height="32" class="rounded-circle me-2" />@User.Identity.Name</a>
                            <ul class="dropdown-menu">
                                <li>
                                    <a class="dropdown-item" asp-page="/UsersPages/Update" asp-route-id="@userId" role="button">Perfil</a>
                                </li>
                                    <li>
                                        <a class="dropdown-item" asp-area="" asp-page="/Jobs/Index">Proyectos</a>
                                    </li>
                                @if (admin)
                                {
                                    <li>
                                        <a class="dropdown-item" asp-area="" asp-page="/ContactMessages/Index">Mensajes</a>
                                    </li>
                                }
                                @if (admin)
                                {
                                    <li>
                                        <a class="dropdown-item" asp-page="/UsersPages/Index">Usuarios</a>
                                    </li>
                                }
                                <li>
                                    <form method="post" asp-page="/Auth/Logout">
                                        <button type="submit" class="dropdown-item">Cerrar Sesion</button>
                                    </form>
                                </li>
                            </ul>
                        </li>
                    }
                    else
                    {
                        <li class="nav-item"><a class="nav-link text-dark"asp-page="/Auth/Login">Login</a></li>
                    }
                </ul>
            </div>
        </div>
    </nav>
</header>
<div class="container">
    <main role="main" class="pb-3">
        @RenderBody()
    </main>
</div>

<footer class="border-top footer text-muted">
    <div class="container">
        &copy; 2026 - Portafolio - <a asp-area="" asp-page="/Privacy">Privacy</a>
    </div>
</footer>

<div class="position-fixed d-flex flex-column gap-3" style="bottom: 30px; right: 30px; z-index: 2000;">
  
    <!-- Botón LinkedIn -->
    <a href="https://www.linkedin.com/in/brian-dorantes-salazar-5ba56888" 
       target="_blank" 
       class="btn btn-primary rounded-circle d-flex align-items-center justify-content-center" 
       style="width: 55px; height: 55px; box-shadow: 0px 4px 10px rgba(0,0,0,0.3);"
       title="Sígueme en LinkedIn">
        <i class="bi bi-linkedin fs-4"></i>
    </a>

    <!-- Botón GitHub -->
    <a href="https://github.com/briandorantes1995" 
       target="_blank" 
       class="btn btn-dark rounded-circle d-flex align-items-center justify-content-center" 
       style="width: 55px; height: 55px; box-shadow: 0px 4px 10px rgba(0,0,0,0.3);"
       title="Visita mi GitHub">
        <i class="bi bi-github fs-4"></i>
    </a>

</div>

<script src="~/lib/jquery/dist/jquery.min.js"></script>
<script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
<script src="~/js/site.js" asp-append-version="true"></script>
<script src="https://cdn.jsdelivr.net/npm/htmx.org@2.0.10/dist/htmx.min.js" integrity="sha384-H5SrcfygHmAuTDZphMHqBJLc3FhssKjG7w/CeCpFReSfwBWDTKpkzPP8c+cLsK+V" crossorigin="anonymous"></script>

@await RenderSectionAsync("Scripts", required: false)
</body>
</html>
